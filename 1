#include <stdio.h>

int main() {
    int x, y;

    printf("Введите значение x: ");
    scanf("%d", &x);

    y = 5 * x + 20;
    y *= 2; // Увеличиваем значение y в два раза

    printf("Значение выражения y = 5 * x + 20 увеличенное в два раза равно: %d\n", y);

    return 0;
}
